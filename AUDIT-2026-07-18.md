# Code Audit - 2026-07-18

- **Target:** `reconcile.py`, `schema/cells.schema.json`; `tests/test_reconcile.py` for the weak-test-assertion entry only.
- **Languages:** Python 3.10+ and JSON Schema.
- **Prior memos:** none found; consequently no finding is a repeat.
- **Scope note:** the `tables/` files are data deliverables, not executable implementation. Their independent corpus-unit source audit is recorded separately in `AUDITS.md`.

## Findings by checklist entry

1. **swallowed-exceptions** - no candidates - checked all production `except` clauses; there are none.
2. **hardcoded-config-values** - no candidates - checked runtime constants and calls; the named tolerance and closure constants are semantic oracle defaults, not silently failing external-service configuration.
3. **missing-network-timeout** - not applicable - no network or subprocess calls.
4. **narrating-comments** - no candidates - comments/docstrings explain oracle semantics, coverage, and error behavior rather than restating adjacent code.
5. **near-identical-siblings** - no candidates - checked the one production module; no three-or-more structurally duplicated blocks.
6. **convention-drift** - no candidates - one runtime module consistently uses `pathlib`, typed collections, and `violations`/`warnings` return values.
7. **inconsistent-error-handling** - not applicable - no sibling adapters, clients, or parallel implementations with comparable error contracts.
8. **mutable-default-arguments** - no candidates - the only function default is immutable `True`.
9. **assert-for-runtime-validation** - no candidates - no production `assert` statements.
10. **async-await-mismatch** - not applicable - no async runtime or coroutine calls.
11. **brittle-error-detection** - no candidates - no exception-string control flow.
12. **f-string-in-logger-call** - not applicable - no logging module or logger calls.
13. **print-instead-of-logging** - false-positive-shape - `main()` prints CLI status and is invoked only by the `if __name__ == "__main__"` entry point; this is explicit command-line output, not internal diagnostics, and the module has no logger.
14. **resource-leak-no-context-manager** - no candidates - the module uses `Path.read_text()` rather than manual file handles; no temp directories, streams, or OS handles are retained.
15. **shell-true-subprocess-injection** - no candidates - no subprocess or shell invocation.
16. **sleep-based-synchronization** - no candidates - no sleeps, retries, or asynchronous setup waits.
17. **string-built-sql** - not applicable - no database or SQL construction.
18. **swapped-args** - no candidates - inspected internal calls; the one optional `check()` argument is supplied by keyword and library calls do not pass ambiguous same-type positional arguments.
19. **tarfile-extractall-without-filter** - not applicable - no archive extraction.
20. **unjustified-lazy-import** - no candidates - all imports are module-level.
21. **unreachable-defensive-guard** - no candidates - the CLI argument-count check protects a public command boundary; no redundant internal guards.
22. **weak-test-assertion** - no candidates - checked all test assertions; they assert exact violation counts, specific error content, or deterministic exit codes rather than loose rendered-content matches.
23. **off-by-one** - no candidates - no range/index arithmetic or N-versus-N-minus-one rate logic in production.
24. **wrong-tool-for-job** - no candidates - `pathlib`, `json`, `Decimal`, and `jsonschema` are appropriate for the stated oracle contract; no general exceptions, manual paths, shell calls, or string-built HTML.

## Compound findings

None.

## Uncategorized observations

None.

## Suggested fix order

No real defects found; no fixes suggested.

## Summary

- real-defect: 0
- false-positive-shape: 1
- legitimate-variant: 0
- not applicable: 6
- no candidates: 17

Every entry in `checklist.md` was covered. No source code was changed.
